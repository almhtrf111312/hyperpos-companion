
## خطة: إضافة مبلغ الضريبة في فاتورة الطباعة وإيصال الواتساب

---

### نظرة عامة
الفاتورة المحفوظة تحتوي بالفعل على `taxAmount` و `taxRate` لكنهما لا يُعرضان في الطباعة ولا في رسالة الواتساب. سنضيف سطر الضريبة في كلا المكانين.

---

### التغييرات المطلوبة

#### 1. تعديل `src/lib/native-share.ts` - إيصال الواتساب

- إضافة `taxAmount?: number` و `taxRate?: number` إلى `InvoiceShareData`
- في `generateInvoiceShareText`: إضافة سطر الضريبة بين الخصم والإجمالي:
  `🧾 الضريبة (15%): $currencySymbol$taxAmount`

#### 2. تعديل `src/pages/Invoices.tsx` - تمرير بيانات الضريبة

**في `handleWhatsApp`:**
- إضافة `taxAmount: invoice.taxAmount` و `taxRate: invoice.taxRate` إلى `shareData`

**في `handlePrint`:**
- إضافة سطر الضريبة بعد سطر الخصم وقبل الإجمالي في HTML الطباعة:
  `${invoice.taxAmount > 0 ? '<div class="tax-row">ضريبة (${invoice.taxRate}%): ${formatCurrency(invoice.taxAmount)}</div>' : ''}`

#### 3. تعديل `src/lib/native-print.ts` - قالب الإيصال

القالب الموجود يدعم بالفعل حقل `tax` في `PrintableInvoice` ويعرضه. لا تغيير مطلوب هنا.

---

### الملفات المعدلة

| الملف | التغيير |
|-------|---------|
| `src/lib/native-share.ts` | إضافة taxAmount/taxRate للواجهة + سطر الضريبة في النص |
| `src/pages/Invoices.tsx` | تمرير taxAmount/taxRate للواتساب + سطر الضريبة في HTML الطباعة |
