# 📋 دليل الإصلاحات والميزات البرمجية المنفذة بواسطة Antigravity

> **تنبيه هام لمطوري الذكاء الاصطناعي (AI Developers / Lovable / Cursor / Assistants):**
> تم تدقيق واعتماد هذه التعديلات والمعادلات الحسابية لتتوافق بدقة مع النظام المحاسبي، حركة المخزون، وورديات الكاشير (Cash Shifts). يرجى **عدم حذف** أو **تعديل** المنطق الحسابي في هذه الملفات لتجنب حدوث عجز وهمي في درج الكاشير أو خلل في احتساب الأرباح والديون.

---

## 1. ربط المرتجعات النقدية بورديات الكاشير (Cash Shifts) ومنع العجز الوهمي

### الملف المعدل:
- [`src/lib/cashbox-store.ts`](src/lib/cashbox-store.ts)

### المشكلة السابقة:
عند قيام الكاشير بعمل استرداد (Refund) لفاتورة نقدية وتسليم المال للعميل من درج الكاشير، كان النظام يعيد المنتجات إلى المخزون لكنه **لا يخصم** المبلغ المسلم للعميل من مبيعات الوردية المفتوحة ولا من رصيد الصندوق. كان هذا يؤدي إلى ظهور "عجز وهمي" في درج الكاشير عند إغلاق الوردية ومطابقة النقد الفعلي.

### الحل المطبق:
1. توسيع دالة `updateCashboxBalance` لتدعم نوع الحركة `'refund'`.
2. إضافة دالة متخصصة:
   ```typescript
   export const recordRefundInShift = (
     amount: number,
     grossProfit: number = 0,
     cogs: number = 0,
     invoiceNumber?: string
   ): void
   ```
3. آلية العمل:
   - خصم المبلغ المرتجع نقداً من رصيد الصندوق النشط (`currentBalance`).
   - خصم المبلغ من إجمالي مبيعات الوردية النشطة المفتوحة (`salesTotal`).
   - عكس إجمالي الربح الإجمالي وتكلفة البضاعة المباعة (`grossProfitTotal`, `cogsTotal`).
   - تسجيل قيد حركة للوردية سحابياً من نوع `refund` عبر `addShiftTransactionCloud`.
   - إطلاق الحدث الفوري `EVENTS.CASH_SHIFTS_UPDATED` لتحديث واجهات الورديات والدرج فوراً.

---

## 2. معالجة مرتجعات فواتير الديون المسددة جزئياً (Partially-Paid Debt Invoices)

### الملفات المعدلة:
- [`src/lib/cloud/invoices-cloud.ts`](src/lib/cloud/invoices-cloud.ts)
- [`src/pages/Invoices.tsx`](src/pages/Invoices.tsx)

### المشكلة السابقة:
إذا كانت الفاتورة عبارة عن "دين" (بيع آجل) وقام العميل سابقاً بتسديد جزء من قيمتها نقداً (مثال: فاتورة بقيمة 100$ سدد منها العميل 40$ نقداً ومتبقي 60$)، فعند استرداد الفاتورة كان النظام يشطب قيد الدين بالكامل، لكن المبلغ الذي دفعه العميل سابقاً (40$) كان يضيع محاسبياً دون توجيه الكاشير لإرجاعه للعميل، ودون خصمه من وردية الكاشير.

### الحل المطبق:
1. عند طلب استرداد فاتورة دين كاملة:
   - يتم فحص `debtPaid` (المبلغ المسدد مسبقاً).
   - إذا كان `debtPaid > 0`، فهذا المبلغ قد دخل سابقاً في حسابات المتجر والوردية، لذا يحدده النظام كـ `cashToRefund`.
   - يتم تلقائياً استدعاء `recordRefundInShift(cashToRefund, ...)` لخصم المبلغ من درج الكاشير للوردية النشطة.
   - يتم شطب الدين المتبقي من جدول `debts`.
2. واجهة المستخدم (`Invoices.tsx`):
   - إظهار شريط تنبيه مالي عالي الوضوح بلون تحذيري يوضح للكاشير:
     - **المسدد سابقاً:** المبلغ الواجب تسليمه نقداً للعميل فوراً.
     - **المتبقي بذمة العميل:** المبلغ الذي سيتم شطبه وإلغاؤه.
     - إشعار صريح بخصم النقد المسترد من الوردية الحالية تلقائياً.

---

## 3. محرك وميزة الاسترداد الجزئي للبضائع (Partial Invoice Refund)

### الملفات المعدلة:
- [`src/lib/cloud/invoices-cloud.ts`](src/lib/cloud/invoices-cloud.ts)
- [`src/pages/Invoices.tsx`](src/pages/Invoices.tsx)

### الميزة المضافة:
إتاحة استرداد بنود معينة وكميات محددة من الفاتورة بدلاً من حصر الكاشير في استرداد كامل الفاتورة فقط.

### التفاصيل التقنية:
1. تم إنشاء دالة سحابية متكاملة:
   ```typescript
   export async function refundInvoicePartialCloud(
     invoiceNumber: string,
     itemsToRefund: PartialRefundItem[],
     reason: string = 'مرتجع جزئي'
   ): Promise<PartialRefundResult>
   ```
2. سلوك المحرك الحسابي والمخزني:
   - التحقق من عدم تجاوز الكمية المرتجعة للكمية المشتراة.
   - إعادة كميات الأصناف المحددة إلى المخزون (`warehouse_stock` أو `products`) مما يفعل تلقائياً قيد حركة المخزون في `stock_movements`.
   - تعديل كميات وإجماليات بنود الفاتورة (`invoice_items`) وحساب المجموع الكلي الجديد وصافي الربح وتكلفة البضاعة.
   - إذا استُردت كل البنود بالكامل، تتحول الفاتورة إلى `status = 'refunded'`.
   - **في فواتير الديون:**
     - يتم تخفيض دين العميل في جدول `debts` بمقدار قيمة المرتجع.
     - إذا تجاوزت قيمة المرتجع الجزئي رصيد الدين المتبقي، يتم شطب الدين بالكامل وإرجاع الفارق نقداً للعميل وخصمه فوراً من الوردية النشطة عبر `recordRefundInShift`.
   - تحديث إحصائيات المشتريات والديون لدى العميل وتسجيل العملية في سجل النشاطات (`activity_logs`).
3. واجهة المستخدم في صفحة الفواتير:
   - نافذة تفاعلية منبثقة مقسمة إلى تبويبين: **استرداد كلي** و **استرداد جزئي**.
   - أزرار زيادة ونقصان للكميات `[+]` و `[-]` مع زر لتحديد الكمية كاملة لكل منتج بضغطة زر.
   - حاسبة مالية فورية للمجاميع والنقد الواجب إرجاعه وتخفيض الدين.

---

## 4. استثناءات تم الحفاظ عليها بناءً على طلب المالك

- **المشكلة رقم 3 (tempId في وضع الأوفلاين):** لم يتم تعديلها أو لمسها.
- **المشكلة رقم 4 (فحص الباركود المكرر):** لم يتم تعديلها أو لمسها.

---

## 5. إرشادات المزامنة السحابية لمطوري الذكاء الاصطناعي (AI Instructions)

1. عند إضافة أي خاصية جديدة تتعلق بالفواتير أو المبيعات، تأكد من استدعاء `EVENTS.INVOICES_UPDATED` و `EVENTS.CASH_SHIFTS_UPDATED`.
2. العمليات الحسابية للأموال يجب دائماً تمريرها عبر دوال التقريب `roundCurrency` و `addCurrency` و `subtractCurrency` لتجنب أخطاء الفاصلة العائمة (Floating Point Precision).
3. جدول `debts` مرتبط بـ `invoice_id`. في حالة التعديل على فاتورة دين، احرص على تعديل `amount` و `remaining_amount` تزامناً مع الفاتورة.

---

## 6. إصلاح خطأ الاسترداد الجزئي للبضائع ("يرجى تحديد كمية للإرجاع أكبر من الصفر")

### الملفات المعدلة:
- [`src/pages/Invoices.tsx`](src/pages/Invoices.tsx)
- [`src/lib/cloud/invoices-cloud.ts`](src/lib/cloud/invoices-cloud.ts)

### المشكلة السابقة:
عند محاولة الكاشير استرداد صنف أو كمية معينة عبر نافذة الاسترداد الجزئي في صفحة الفواتير، كانت تظهر رسالة خطأ:
> *"يرجى تحديد كمية للإرجاع أكبر من الصفر"*
على الرغم من أن الكاشير حدد كمية صالحة (مثلاً 1 أو أكثر).
ظن البعض أن المشكلة من الباك إند المغلق، ولكن بالفحص الدقيق تبين أن سببها هو عدم تطابق تسمية الحقول (Payload Mismatch) بين الفرونت إند والباك إند المحلي:
- في `Invoices.tsx` كانت الدالة تجمع البنود وترسل الحقل باسم `quantity`.
- في `invoices-cloud.ts` كانت الدالة تبحث وتفلتر حصراً بالحقل `item.quantityToRefund > 0`. ونظراً لأن `quantityToRefund` كانت `undefined`، كانت تفرغ المصفوفة وتطلق هذا الخطأ فوراً.

### الحل المطبق:
1. في `Invoices.tsx`:
   - تم تعديل كائن الإرسال ليتضمن كلا المسميين: `quantityToRefund: qty` و `quantity: qty`، مع احتساب `profit` الدقيق.
2. في `invoices-cloud.ts`:
   - توسيع واجهة `PartialRefundItem` لقبول `quantityToRefund` أو `quantity` كبدائل معتمدة.
   - فلترة العناصر بعد استخراج القيمة العددية عبر:
     ```typescript
     quantityToRefund: Number(item.quantityToRefund ?? item.quantity) || 0
     ```
   - تحسين مطابقة البنود مع الفاتورة الأصلية بحيث تدعم المطابقة عبر `productId` أو `id` أو اسم المنتج `productName` كبديل آمن لتجنب أي فشل في العثور على الصنف.

---

## 7. الترقية الشاملة لفواتير المشتريات والشراء السريع وربطها التلقائي بكتالوج المنتجات والمخزون

### الملفات المعدلة والمنشأة:
- [`src/components/products/QuickPurchaseDialog.tsx`](src/components/products/QuickPurchaseDialog.tsx)
- [`src/components/products/PurchaseInvoiceDialog.tsx`](src/components/products/PurchaseInvoiceDialog.tsx)
- [`src/lib/cloud/purchase-invoices-cloud.ts`](src/lib/cloud/purchase-invoices-cloud.ts)
- [`src/lib/cloud/purchase-queue-processor.ts`](src/lib/cloud/purchase-queue-processor.ts)

### المشكلة السابقة:
1. كانت نافذة "الشراء السريع" (`QuickPurchaseDialog`) تقتصر على 3 حقول بدائية جداً (اسم الصنف، الكمية، سعر الشراء). لم يكن هناك إمكانية لإدخال الباركود، الفئة، سعر البيع المقترح، سعر الجملة، الحد الأدنى للتنبيه، أو تاريخ انتهاء الصلاحية.
2. **الخلل الأكبر:** كانت عمليات الشراء السريع تحفظ الفاتورة في جدول `purchase_invoices` وبنودها في `purchase_invoice_items` **دون أن تقوم بإنشاء أو تحديث أي منتج في جدول المنتجات `products` أو المخزون الفعلي!** ونتيجة لذلك، كانت البضاعة المشتراة لا تظهر للمستخدم في شاشة الكاشير (POS) ولا في قائمة المنتجات والمخزون.
3. في نافذة فواتير المشتريات الكاملة (`PurchaseInvoiceDialog`)، كانت تفاصيل الصنف الإضافية (مثل سعر الجملة، الصلاحية، إعدادات الكراتين) تسقط ولا تحفظ عند إنشاء المنتج الجديد.

### الحل المطبق:
1. **ترقية نافذة الشراء السريع (`QuickPurchaseDialog.tsx`):**
   - **البحث والإكمال التلقائي للمنتجات الحالية:** بمجرد كتابة حرفين من اسم المنتج أو مسح الباركود، يبحث النظام فوراً في المنتجات المسجلة في المخزون. وفي حال اختياره، يتم ملء بياناته تلقائياً وإظهار شارة خضراء مميزة: *"منتج مسجل مسبقاً: (المخزون الحالي: X)"*، مع إمكانية إلغاء التحديد لإضافة منتج جديد.
   - **حقل الباركود والماسح الضوئي:** إمكانية كتابة الباركود أو تشغيل ماسح الكاميرا (`BarcodeScanner`) لمسح الباركود بدقة.
   - **قائمة الفئات والتصنيفات:** اختيار الفئة من القائمة السحابية للمتجر (`getCategoryNamesCloud`) أو كتابة فئة جديدة.
   - **هيكلية التسعير المتكاملة:** أسعار متجاورة لسعر الشراء (التكلفة)، سعر البيع المقترح، وسعر الجملة.
   - **محددات المخزون والصلاحية:** إدخال الحد الأدنى للمخزون وتاريخ انتهاء الصلاحية.
   - **دعم وحدات التجزئة والكرتونة:** إمكانية تحديد اسم كرتونة/قطعة ومعامل التحويل وسعر الكرتونة جملة وتجزئة.
   - **التقاط أو رفع صورة المنتج / الفاتورة:** دعم الكاميرا المباشرة أو الاختيار من المعرض.
   - **الربط المحاسبي والمخزني التلقائي:**
     - إذا كان المنتج مسجلاً مسبقاً: يضاف العدد المشتري لمخزونه فوراً، ويُعاد احتساب متوسط التكلفة المرجح (Weighted Average Cost)، وتُسجل الحركة في تاريخ مشتريات المنتج `purchase_history`.
     - إذا كان المنتج جديداً: يتم تسجيله فوراً كبطاقة منتج متكاملة بجدول `products` بكافة تفاصيله الغنية وكميته، ليظهر فوراً في نقاط البيع وشاشات الجرد.
     - يتم حفظ الفاتورة وبنودها في جداول `purchase_invoices` و `purchase_invoice_items` مع ربط الصنف بـ `product_id`.
     - دعم كامل لنمط العمل أوفلاين بدون إنترنت وحفظ العملية في طابور المزامنة `sync-queue`.
2. **ترقية نافذة فواتير المشتريات الكاملة (`PurchaseInvoiceDialog.tsx`):**
   - ترقية دالة `handleAddItem` لاستقبال وتمرير جميع الحقول الموسعة (باركود، فئة، حد أدنى للمخزون، صلاحية، سعر جملة، كراتين).
   - التحقق الفوري من وجود بطاقة للمنتج في المخزون أو إنشائها فوراً مع ربط الصنف بالـ `product_id`، لضمان تزامن دقيق 100% بين الفاتورة والمخزن.
   - إطلاق الحدث `EVENTS.PRODUCTS_UPDATED` فور اعتماد الفاتورة لتحديث جداول المنتجات وشاشات المبيعات تلقائياً.
3. **ترقية معالج طابور المشتريات أوفلاين (`purchase-queue-processor.ts`):**
   - تمكين معالجة المشتريات السريعة عند عودة الإنترنت من تحديث المخزون وإدراج بطاقات الأصناف الجديدة بجميع بياناتها.

---

## 8. حل مشكلة اختفاء أيقونة التطبيق أثناء/بعد التثبيت كـ APK (App Icon Fix)

### الملفات المعدلة:
- حذف: `android/app/src/main/res/drawable-v24/ic_launcher_foreground.xml`
- حذف: `android/app/src/main/res/drawable/ic_launcher_background.xml`
- توليد وتحديث: `android/app/src/main/res/mipmap-*/ic_launcher.png` و `ic_launcher_round.png` و `ic_launcher_foreground.png`
- السكربت المولد: [`scripts/generate-icons.ps1`](scripts/generate-icons.ps1)

### سبب المشكلة:
كان مشروع أندرويد يحتوي على ملف فيكتور افتراضي قديم `drawable-v24/ic_launcher_foreground.xml` يحمل رأس روبوت أندرويد باللون الأبيض (`#FFFFFF`)، موضوعاً فوق خلفية بيضاء (`#FFFFFF`). ونظراً لأولويات نظام أندرويد للموارد في مجلد `drawable-v24` (للأجهزة الحديثة بنظام أندرويد 7 فما فوق)، كان هذا الملف الوهمي يلغي الأيقونة الحقيقية ويجعلها تظهر ككتلة بيضاء مختفية أو لا تظهر على الهاتف أثناء التثبيت.

### الحل المطبق:
1. تم حذف ملفات الفيكتور الافتراضية المتعارضة.
2. تم استخدام الأيقونة الرسمية الأصلية عالية الدقة من `resources/icon.png` (1024x1024) وتوليد كافة حزم الأيقونات بدقة متناهية لكافة كثافات الشاشات:
   - `mipmap-mdpi` (48x48 / 108x108)
   - `mipmap-hdpi` (72x72 / 162x162)
   - `mipmap-xhdpi` (96x96 / 216x216)
   - `mipmap-xxhdpi` (144x144 / 324x324)
   - `mipmap-xxxhdpi` (192x192 / 432x432)
   مع احتساب منطقة الأمان (Safe Zone 70%) لطبقة الواجهة التكيفية `ic_launcher_foreground` لضمان ظهور الشعار كاملاً دون أي اقتطاع في الواجهات الدائرية والمربعة.

---

## 9. حل تنبيه "هل تريد التثبيت عبر USB؟" وبناء حزمة Release موقعة دائماً (Fix USB Install Prompt)

### الملف المعدل:
- [`.github/workflows/build-apk.yml`](.github/workflows/build-apk.yml)

### سبب المشكلة:
في سير العمل السابق، إذا لم تكن مفاتيح التوقيع السرية `KEYSTORE_BASE64` مضافة في إعدادات المستودع (GitHub Secrets)، كان سير العمل يلجأ تلقائياً إلى بناء حزمة تصحيح أخطاء:
```bash
./gradlew assembleDebug
```
وحزم الـ Debug تحمل بطبيعتها سمة `android:debuggable="true"` وموقعة بمفتاح الاختبار الافتراضي لأندرويد. وعند تثبيتها يدوياً كملف على أجهزة أندرويد (وخاصة هواتف شاومي/ريدمي/بوكو بنظام MIUI/HyperOS أو سامسونج)، تكتشف أداة تثبيت الحزم أنها حزمة تصحيح وتطلق تنبيهاً أمنياً محيراً:
> *"هل تريد التثبيت عبر USB؟"*
وتعامل التطبيق كأنه مرسل عبر كابل المطورين (ADB / USB Debugging).

### الحل المطبق:
تم تحديث سير العمل في GitHub Actions ليعتمد دائماً وأبداً على بناء حزمة إنتاجية موقعة (`assembleRelease`):
1. عند توفر المفتاح السري المخصص في المستودع يتم استخدامه.
2. في حال عدم وجود مفتاح سري، يقوم السيرفر تلقائياً وبشكل فوري بتوليد مفتاح توقيع إنتاجي رسمي عبر أداة `keytool` القياسية:
   ```bash
   keytool -genkeypair -v -keystore android/app/flowpos-release.keystore -alias flowpos ...
   ```
3. يتم بناء حزمة الإنتاج الموقعة:
   ```bash
   ./gradlew assembleRelease
   ```
4. الحزمة الناتجة تكون `android:debuggable="false"` وموقعة توقيعاً سليماً، ويتم تثبيتها كأي ملف APK نظامي دون ظهور أي رسالة أو طلب للتثبيت عبر USB نهائياً.

---

## 10. تحسين واجهة شاشة شروط الاستخدام والخصوصية للموبايل (Privacy Policy UI Upgrade)

### الملف المعدل:
- [`src/components/PrivacyPolicyScreen.tsx`](src/components/PrivacyPolicyScreen.tsx)

### المشاكل السابقة:
1. الأيقونة العلوية كانت تظهر ممطوطة رأسياً (شكل بيضاوي غير متناسق) على بعض الهواتف المحمولة بسبب تمدد الـ Flex وعدم تثبيت نسبة العرض إلى الارتفاع.
2. مؤشر التمرير كان عبارة عن نص عادي صغير ولم يكن هناك سهم أو شريط ينبه المستخدم للنزول لتفعيل زر الموافقة.

### الحل المطبق:
1. **تثبيت أبعاد الدائرة بدقة تامة:**
   تم ضبط شارة الشعار العلوية لتكون دائرة هندسية كاملة بنسبة `aspect-square` و `rounded-full` وبأبعاد ثابتة `56px x 56px` مع حد ناعم وظل خفيف، بحيث يستحيل أن تتمدد أو تتشوه على أي شاشة هاتف.
2. **إضافة شريط تفاعلي مع سهم متحرك يقفز (`animate-bounce`):**
   - تم إنشاء شريط تفاعلي بارز في الأسفل يحمل أيقونة سهم متحركة تقفز للأسفل مع عبارة واضحة:
     > *"انزل للأسفل لقراءة الشروط وتفعيل خيار الموافقة ⬇"*
   - الشريط قابل للنقر: بمجرد الضغط عليه، يقوم بالتمرير السلس والتلقائي (`scrollToBottom`) فوراً إلى نهاية الشروط.
3. **تأكيد الوصول وتفعيل الموافقة:**
   بمجرد وصول المستخدم إلى أسفل الشروط، يتحول الشريط إلى تنبيه أخضر أنيق:
   > *"✓ تمت قراءة الشروط بنجاح — يمكنك الآن تأكيد الموافقة أدناه"*
   ويصبح مربع الاختيار مفعلاً وواضحاً، وتتيح البطاقة النقر في أي مكان عليها لاختيار الموافقة وتفعيل زر المتابعة.

---

## 11. أتمتة بناء الـ APK تلقائياً عند كل رفع (GitHub Actions Automation)

### الملف المعدل:
- [`.github/workflows/build-apk.yml`](.github/workflows/build-apk.yml)

### المشكلة السابقة:
كان سير العمل يعمل فقط عند إنشاء وسوم الإصدارات (`tags: ['v*']`) أو عبر تشغيله يدوياً عبر زر `workflow_dispatch`، مما كان يضطر المستخدم للدخول إلى صفحة Actions يدوياً لتشغيله.

### الحل المطبق:
تمت إضافة مراقبة الفرع الرئيسي `main`:
```yaml
on:
  push:
    branches: [ main ]
    tags: ['v*']
  workflow_dispatch:
```
والآن، في كل مرة يتم فيها دفع تعديلات (`git push origin main`)، يبدأ GitHub Actions تلقائياً ببناء تطبيق الأندرويد وتجهيز ملف الـ APK ورفعه كـ Artifact دون أي تدخل يدوي إطلاقاً.
