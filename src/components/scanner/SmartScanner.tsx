import { BarcodeScanner } from '../BarcodeScanner';

/**
 * SmartScanner
 * Wrapper for the unified BarcodeScanner component (ML Kit + Web Fallback).
 * Created to satisfy the specific architecture requirement.
 */
export const SmartScanner = BarcodeScanner;
